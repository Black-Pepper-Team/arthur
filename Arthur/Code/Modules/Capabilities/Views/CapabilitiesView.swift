import SwiftUI

struct CapabilitiesView: View {
    var body: some View {
        VStack {
            Text("AI Capabilities")
                .font(.custom(Fonts.interBold, size: 35))
            Spacer()
        }
    }
}

#Preview {
    CapabilitiesView()
}
